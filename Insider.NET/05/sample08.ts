var discount: number;
var price: number = 1000;
var rank: string = "gold";

discount = (rank == "gold" ? price * 0.2 : price * 0.1);
alert(discount);var discount: number;
var price: number = 1000;
var rank: string = "gold";

discount = (rank == "gold" ? price * 0.2 : price * 0.1);
alert(discount);